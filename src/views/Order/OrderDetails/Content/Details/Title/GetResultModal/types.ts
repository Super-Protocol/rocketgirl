export interface GetResultModalProps {
    orderAddress: string;
}

export interface FormValues {
    phrase?: string;
}

export enum Fields {
    phrase = 'phrase',
    agreement = 'agreement',
    phraseTabMode = 'phraseTabMode',
}
