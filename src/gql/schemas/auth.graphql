query AuthChecker {
    result: providers (pagination: { first: 1 }, filter: {}) {
        page {
            edges {
                node {
                    _id
                }
            }
        }
    }
}
