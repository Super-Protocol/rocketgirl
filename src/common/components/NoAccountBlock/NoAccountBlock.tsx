import React, { memo, FC } from 'react';

import { NoAccountBlockProps } from './types';

export const NoAccountBlock: FC<NoAccountBlockProps> = memo(() => {
    return <div>NoAccountBlock</div>;
});
